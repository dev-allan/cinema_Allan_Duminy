import React, { Fragment } from 'react';
import Routing from './Navigation/Routing';

export const App = () => {
    return (
        <Fragment>
            <Routing/>
        </Fragment>
    );
};

export default App;